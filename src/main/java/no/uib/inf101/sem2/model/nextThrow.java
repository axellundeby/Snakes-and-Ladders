package no.uib.inf101.sem2.model;

public interface nextThrow {
  
     /**@return An integer value between 1 and 6, which represents the number of eyes that show up on the rolled dice. */

    int rollDice();
   
}
